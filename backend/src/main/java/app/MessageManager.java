package app;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class MessageManager {
    private static final Logger log = LoggerFactory.getLogger(MessageManager.class);
    private Application app;
    public MessageManager(Application app) {
        this.app = app;
    }

    //Functionality: send message, delete message, read message from entire chat
    //Also when clicking on messages should open up a list of past messages with all people you messaged




}
